<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nanosheep.bikeroute" android:versionName="@string/version" android:versionCode="1">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    

    <application android:label="@string/app_name"
            android:icon="@drawable/bikeroute" android:testOnly="true">

        <uses-library android:name="com.google.android.maps" />

       <activity android:name="com.nanosheep.bikeroute.BikeNav" android:label="BikeRoute">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"></category>
                <action android:name="android.intent.action.MAIN"></action>
            </intent-filter>
       </activity>
       <activity android:name="com.nanosheep.bikeroute.FindPlace" android:label="Find Address">
       </activity>
       <activity android:name="com.nanosheep.bikeroute.DirectionsView" android:label="Directions">
       </activity>
   </application>

</manifest>
